var Backoff=require("..");var assert=require("assert");describe(".duration()",function(){it("should increase the backoff",function(){var a=new Backoff;assert(100==a.duration());assert(200==a.duration());assert(400==a.duration());assert(800==a.duration());a.reset();assert(100==a.duration());assert(200==a.duration())})});